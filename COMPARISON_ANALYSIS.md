# Godot Pet 项目新旧版本对比分析报告

## 概述

本文档对比分析了 `GAME/godot-pet` (新版本) 和 `GAME/godot-pet-origin/godot_pet` (旧版本) 的主要差异，重点关注动作和动画系统相关的改动，并识别潜在的bug。

## 主要差异总结

### 1. 项目架构和代码结构

#### 新版本特点
- **模块化架构重构**：强调"100% 还原原始动画循环与物理同步逻辑，同时保持模块化架构"
- **战术解耦设计**：物理模块采用战术解耦设计，不侵入核心控制器逻辑
- **日志去重机制**：添加了日志去重功能，避免重复日志刷屏
- **简化注释风格**：使用简洁的注释格式和代码分组

#### 旧版本特点
- **复杂的状态管理**：包含更多马尔可夫性相关的状态管理逻辑
- **丰富的程序化动画**：支持更多类型的程序化动画（wave, spin, bounce, fly, roll, shake, flip, dance等）
- **直接的状态处理**：EQS查询直接在控制器中处理

### 2. 动画系统差异

#### 重大变更：动画系统大幅简化

**旧版本动画系统特点：**
```gdscript
# 支持丰富的程序化动画
func is_procedural_anim(name: String) -> bool:
    return name in ["wave", "spin", "bounce", "fly", "roll", "shake", "flip", "dance"]

# 复杂的BlendTree参数管理
func apply_blendtree_state(state: int) -> void:
    # 包含locomotion blend_position等多种参数
```

**新版本动画系统特点：**
```gdscript
# 只支持基础程序化动画
func is_procedural_anim(n: String) -> bool:
    return n in ["fly", "wave", "flip", "spin"]

# 简化的BlendTree参数管理
func apply_blendtree_state(state: int) -> void:
    # 只管理jump_blend
```

#### ⚠️ **潜在Bug 1：程序化动画功能缺失**

**问题描述：**
新版本移除了以下程序化动画支持：
- `spin` (旋转)
- `bounce` (弹跳)
- `roll` (翻滚)
- `shake` (颤抖)
- `dance` (舞蹈)

**影响：**
- 服务端发送这些动画指令时，新版本客户端无法正确响应
- 宠物行为表现不如旧版本丰富

**修复建议：**
恢复对这些程序化动画的支持，或确保服务端不会发送不支持的动画指令。

#### ⚠️ **潜在Bug 2：动画状态切换逻辑简化**

**问题描述：**
新版本的 `switch_anim` 函数移除了对动画树激活状态的管理逻辑：

```gdscript
# 新版本：程序化动画时不管理动画树状态
if is_procedural_anim(normalized_name):
    set_procedural_anim(normalized_name)
    proc_time = 0.0
    return
```

**旧版本的逻辑更复杂：**
```gdscript
if is_procedural_anim(normalized_name):
    if animation_tree:
        animation_tree.set("parameters/locomotion/blend_position", 0.0)
        animation_tree.set("parameters/jump_blend/blend_amount", 0.0)
    apply_blendtree_state(PetData.AnimState.IDLE)
    current_anim_state = PetData.AnimState.IDLE
    set_procedural_anim(normalized_name)
    proc_time = 0.0
    return
```

**潜在影响：**
- 程序化动画切换时可能出现动画树状态不一致
- 基础动画向程序化动画过渡时可能有视觉跳跃

### 3. 物理系统差异

#### 新增功能：战术解耦设计

**新版本添加的战术逻辑：**
```gdscript
# 战术状态变量
var _jump_push_pending: bool = false
var _jump_start_height: float = 0.0
var _jump_phase_2_threshold: float = 0.2
var _mid_air_push_speed: float = 5.0

# 空中战术前冲逻辑
func process_tactical_logic(character_body: CharacterBody3D):
    if _jump_push_pending and not character_body.is_on_floor() and character_body.velocity.y > 0:
        # 空中前冲逻辑实现
```

#### ⚠️ **潜在Bug 3：跳跃行为改变**

**问题描述：**
新版本的跳跃速度从 `6.5` 改为 `8.0`，并且添加了复杂的战术逻辑。

**潜在影响：**
- 跳跃高度和行为可能与旧版本不一致
- AI控制的跳跃行为可能过于激进
- 空中前冲逻辑可能导致宠物"飞出"预期轨迹

**修复建议：**
测试跳跃行为的一致性，确保战术逻辑不会影响正常的用户控制跳跃。

### 4. 消息处理系统差异

#### EQS查询处理方式改变

**旧版本：**
```gdscript
func _on_eqs_query_received(data: Dictionary) -> void:
    # 直接在控制器中处理EQS查询
    var query_id = data.get("query_id", "")
    var config = data.get("config", {})
    # 直接构建上下文并执行查询
    eqs_adapter.execute_query(query_id, config, context)
```

**新版本：**
```gdscript
func _on_eqs_query_received(d: Dictionary) -> void:
    # 通过适配器处理
    eqs_adapter.handle_query(d)
```

**新增的适配器方法：**
```gdscript
func handle_query(query_data: Dictionary) -> void:
    var query_id = query_data.get("query_id", "")
    var data = query_data.get("data", {})
    var config = data.get("config", {})
    var context = data.get("context", {})
    execute_query(query_id, config, context)
```

#### ⚠️ **潜在Bug 4：消息格式兼容性**

**问题描述：**
新版本假设EQS查询消息格式为：
```json
{
  "query_id": "xxx",
  "data": {
    "config": {...},
    "context": {...}
  }
}
```

但旧版本直接处理的消息格式可能不同。

**潜在影响：**
- 如果服务端仍使用旧格式发送消息，新版本可能无法正确解析
- EQS查询失败导致"飞到小球旁边，eqs没查到"的问题

#### ⚠️ **Bug 5：鼠标输入处理函数缺失（已修复）**

**问题描述：**
新版本完全移除了鼠标输入处理函数：
- `_input_event` - 处理角色身上的点击事件
- `_input` - 处理拖拽状态下的鼠标事件
- `_unhandled_input` - 处理地面点击事件

**根本原因：**
重构过程中过度简化，删除了必要的输入处理入口。

**修复措施：**
已恢复所有三个输入处理函数，确保鼠标左键点击功能正常工作。

#### ⚠️ **Bug 6：动态场景中飞行动作失败（已修复）**

**问题描述：**
在动态场景执行（如飞到舞台上）时，fly动作无法正确执行，宠物无法到达目标位置。

**根本原因：**
1. fly动作没有调用程序化动画，只是设置了is_flying标志
2. 程序化动画的fly实现与动态场景的位置插值产生冲突

**修复措施：**
1. 修改fly动作处理，调用`animation_module.switch_anim("fly")`来启动程序化动画
2. 优化程序化动画fly的实现，使其适合动态场景使用（减小悬浮幅度，避免与位置插值冲突）

#### ⚠️ **Bug 7：跳跃后左右移动延迟（已修复）**

**问题描述：**
跳跃后，左右移动有明显的延迟和不响应。

**根本原因：**
1. 服务器jump指令错误地启用了空中战术前冲功能
2. 战术前冲逻辑强制设置角色速度，覆盖用户输入

**修复措施：**
1. 服务器jump指令不再启用空中战术前冲
2. 战术逻辑增加用户输入检测，有输入时不执行自动前冲

### 5. 信号连接方式差异

#### 从Lambda函数改为独立函数

**旧版本：**
```gdscript
interaction_module.interaction_sent.connect(func(action, data):
    messaging_module.send_interaction(action, data, global_position)
)
```

**新版本：**
```gdscript
interaction_module.interaction_sent.connect(_on_interaction_sent)
# ...
func _on_interaction_sent(action: String, data: Dictionary) -> void:
    messaging_module.send_interaction(action, data, global_position)
```

**影响：**
- 代码更清晰，但需要确保所有信号都有对应的处理函数
- Lambda函数的闭包特性丢失，可能影响某些动态行为

### 6. 日志系统改进

#### 新增日志去重机制

**新版本添加：**
```gdscript
var log_history: Dictionary = {}
const LOG_COOLDOWN_MS = 3000

func _log(msg: String) -> void:
    var now = Time.get_ticks_msec()
    if not ("phase" in msg or "Executing" in msg) and log_history.has(msg) and now - log_history[msg] < LOG_COOLDOWN_MS:
        return
    log_history[msg] = now
    print("[%s] %s" % [Time.get_time_string_from_system(), msg])
```

**优势：**
- 避免重复日志刷屏
- 提升调试体验

**潜在问题：**
- 某些重要但重复的日志可能被过滤
- 内存中保留的日志历史可能会不断增长

## 关键Bug和修复建议

### 🔴 **高优先级Bug**

1. **程序化动画功能缺失**
   - **影响**：宠物行为表现不完整
   - **修复**：恢复对缺失动画的支持或更新服务端配置

2. **EQS查询消息格式不兼容**
   - **影响**：环境查询功能失效
   - **修复**：验证消息格式并添加兼容性处理

3. **鼠标输入处理函数缺失** ✅ **已修复**
   - **影响**：鼠标左键点击功能完全失效
   - **修复**：恢复 `_input_event`、`_input` 和 `_unhandled_input` 函数

### 🟡 **中优先级Bug**

4. **动画状态切换逻辑简化**
   - **影响**：动画过渡可能不平滑
   - **修复**：恢复动画树状态管理逻辑

5. **跳跃行为改变**
   - **影响**：物理行为不一致
   - **修复**：调整跳跃参数确保一致性

### 🟢 **低优先级问题**

5. **日志去重可能过滤重要信息**
   - **影响**：调试困难
   - **修复**：优化去重逻辑，允许关键日志通过

## 建议的修复顺序

1. **已修复**：鼠标输入处理函数，恢复点击功能 ✅
2. **已修复**：动态场景飞行动作，解决舞台飞行问题 ✅
3. **已修复**：跳跃后移动延迟，恢复流畅控制 ✅
4. **立即修复**：EQS查询处理，确保基础功能正常
5. **优先修复**：恢复关键的程序化动画支持
6. **逐步优化**：完善动画过渡逻辑和物理行为一致性
7. **持续改进**：优化日志系统和代码结构

## 测试建议

1. **功能测试**：
   - 验证所有程序化动画是否正常工作
   - 测试EQS查询功能（飞到小球旁边）
   - 检查跳跃和移动行为一致性

2. **性能测试**：
   - 监控内存使用（日志历史增长）
   - 检查动画切换性能

3. **兼容性测试**：
   - 验证与服务端消息格式的兼容性
   - 测试各种用户交互场景

---

**文档生成时间**：2026年1月17日
**分析版本**：新版本 vs 旧版本对比
**主要发现**：动画系统大幅简化，存在功能缺失风险
**最新更新**：修复鼠标输入、动态场景飞行和跳跃移动延迟问题